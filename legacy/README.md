# legacy/

The first prototype of CLARA, built against a small synthetic dataset before the real
HHGOA data was available (`agent.py`, `graph_client.py`, `voi.py`, `run_benchmark.py`, B-01..B-05).
It is kept for reference only and is not used by the submission pipeline
(`run_cases.py` → `clara/investigator.py`).
